using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TextMovementTextEngine : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        int x = 0;
        while (x < 10)
        {
           // AddToQueue("Test Text " + x);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    IEnumerator testDirectionalOutput()
    {


        yield return null;
    }

}
